// ex
bool checkEven(int value) {
  return value % 2 == 0;
}

// các cách viết khác cho kiểu trên

// 1 bool checkEven(int value) => value % 2 == 0;

// bool checkEvent(int value) => someOtherFunction(value);

